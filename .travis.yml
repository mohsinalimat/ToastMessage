language: swift
xcode_project: ToastMessage.xcodeproj
xcode_scheme: ToastMessageTests

branches:
  only:
    master

script: xcodebuild -project ToastMessage.xcodeproj -scheme ToastMessage -sdk iphonesimulator test ONLY_ACTIVE_ARCH=NO